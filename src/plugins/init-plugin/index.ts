import { readFileSync } from "fs";
import { open } from "../../tools";
import { IFeCliPlugin } from "../../interface";
import { select } from "@inquirer/prompts";

/**
 * 各种项目的脚手架
 * fe init
 * fe init plugin:
 */
export default (async function InitPlugin(args) {

    select
    

} as IFeCliPlugin);
